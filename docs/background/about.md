## Why Flow?

In short, the need for a lightweight configurable and auditable transformation approach integrated within a Spark
application wasn't being met by other identified solutions.  Many full-fledged products required additional infra, 
or required using Python instead of configuration through data suitable for audit and end-user maintenance.

Flow aims to fill the 'in process' data processing library gap configurable via simple sql snippets and some json.